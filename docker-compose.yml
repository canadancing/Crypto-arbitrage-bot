version: "3.8"

services:
  gatebot:
    build: .
    container_name: gatebot
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./logs:/app/logs
